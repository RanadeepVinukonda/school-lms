{
  "buildCommand": "npm run build:backend && npm run build:frontend",
  "installCommand": "npm install --prefix backend --include=dev && npm install --include=dev && npm install --prefix frontend --include=dev",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
